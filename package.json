{
  "name": "pt-gen-refactor",
  "author": "Hares",
  "version": "1.0.7",
  "description": "Cloudflare Worker for PT-Gen Refactor",
  "main": "worker/index.js",
  "scripts": {
    "dev": "cd worker && npx wrangler dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm run build && cd ../worker && npx wrangler deploy"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "pt-gen"
  ],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.33.2"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
